#include "defines.h"
#include <cmath>
#include <iostream>
#include <vector>
#include "array_utils.h"

int main() {

	std::vector<int> test();

	for (int i=0;i<=std::log2(N);i++) {
		for(int j=i-1;j>=0;j--) {
			for (int k=0;k<=N;k++) {
				
			}
		}	
	}
}
